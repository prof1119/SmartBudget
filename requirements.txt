# SmartBudget — Python dependencies
# ─────────────────────────────────
# Core
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1

# ─────────────────────────────────
# Uncomment when connecting to PostgreSQL
# ─────────────────────────────────
# asyncpg==0.29.0          # async PostgreSQL driver
# databases==0.9.0         # async query layer (works with asyncpg)
# sqlalchemy==2.0.30       # ORM / schema definitions (optional)
# alembic==1.13.1          # DB migrations
